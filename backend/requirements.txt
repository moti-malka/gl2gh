# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.31.1
python-multipart>=0.0.22

# Database
motor==3.3.2
pymongo==4.6.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Pin to 4.0.1 for passlib 1.7.4 compatibility (bcrypt 4.1+ breaks passlib)
python-dotenv==1.0.0

# Job Queue
celery>=5.3.0
redis>=6.4.0

# HTTP client for agents
httpx>=0.27.1
requests==2.31.0

# Validation and schemas
pydantic>=2.5.0
pydantic-settings>=2.5.2
email-validator==2.1.0

# Encryption
cryptography==46.0.5

# JSON and YAML
jsonschema==4.20.0
PyYAML==6.0.1

# WebSocket and SSE support
websockets>=15.0.1
python-socketio>=5.10.0
sse-starlette>=2.0.0

# Logging
python-json-logger==2.0.7

# Microsoft Agent Framework (pre-release)
# Install with: pip install -r requirements.txt --pre
# Using core only to avoid dependency conflicts with [all] extras
agent-framework-core>=1.0.0b260128
azure-identity>=1.15.0
azure-core>=1.29.0

# GitHub API client
PyGithub>=2.1.1

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
