apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: discovery-agent-data
  namespace: discovery-agent
  labels:
    app: discovery-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard  # Update based on your cluster
