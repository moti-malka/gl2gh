apiVersion: v1
kind: Secret
metadata:
  name: discovery-agent-secrets
  namespace: discovery-agent
type: Opaque
stringData:
  # Replace these with actual values or use external secret management
  GITLAB_TOKEN: "your-gitlab-token-here"
  GITLAB_BASE_URL: "https://gitlab.com"
  GITLAB_ROOT_GROUP: ""  # Optional
---
# Alternative: Use external secrets operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: discovery-agent-secrets
#   namespace: discovery-agent
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: discovery-agent-secrets
#   data:
#     - secretKey: GITLAB_TOKEN
#       remoteRef:
#         key: discovery-agent/gitlab
#         property: token
#     - secretKey: GITLAB_BASE_URL
#       remoteRef:
#         key: discovery-agent/gitlab
#         property: base_url
